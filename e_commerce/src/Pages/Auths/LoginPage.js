import React from 'react';

import { Container } from '@mui/material';
import Form from './../../Components/Form';

const LoginPage = () => {
    
    return (
        <Container>
            <Form title={"Login"} buttonText={"Login"} />
        </Container>
    );
};

export default LoginPage;